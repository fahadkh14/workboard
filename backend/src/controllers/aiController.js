import Task from "../models/Task.js";import Project from "../models/Project.js";import {asyncHandler} from "../utils/asyncHandler.js";import {generateAssistantReply} from "../services/aiService.js";
export const chat=asyncHandler(async(req,res)=>{const id=req.user._id,scope="(createdById=? OR assigneeId=?)";const [taskCount,overdueCount]=await Promise.all([Task.countWhere(`${scope} AND status<>'completed'`,[id,id]),Task.countWhere(`${scope} AND status<>'completed' AND dueDate IS NOT NULL AND dueDate<NOW()`,[id,id])]);const projectCount=(await Project.findVisible(id)).length;res.json({success:true,reply:await generateAssistantReply({message:req.body.message,context:{taskCount,overdueCount,projectCount}})})});
